hostname {{ hostname }}
!
vlan {{ vlan_id }}
 name {{ vlan_name }}
!
interface {{ interface_name }}
 description {{ description }}
 switchport mode access
 switchport access vlan {{ vlan_id }}
!
end
write memory
